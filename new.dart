import 'dart:io';

void main() {
  // stdout.write("Enter Vistiors Name :");
  // var name =  stdin.readLineSync();

  // stdout.write("Enter PRODUCT Title :");
  // var prod_title =  stdin.readLineSync();

  // stdout.write("Enter Quantity  :");
  // int? data =  int.parse(stdin.readLineSync()!);

  // print(" ${name} ordered ${data} ${prod_title} on XYZ Clothing store");
  // var name =  stdin.readLineSync();
  // var data = int.tryParse(name ?? "");
  // print(data);
  // stdout.write("Enter Number 1 :");
  // int n1 = int.parse(stdin.readLineSync()!);

  int tk = 600;
  int qty = int.parse(stdin.readLineSync()!);
  print("Price : ${tk*qty}");

  

  // stdout.write("Enter  :");
  // int n2 = int.parse(stdin.readLineSync()!);  //input

  // double F = (n2* 9 / 5) + 32;
  // print(F);
}
